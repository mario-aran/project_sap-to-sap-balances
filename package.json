{
  "name": "sap-to-sap-balances",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "find . -type d \\( -name 'node_modules' \\) -prune -exec rm -rf {} +",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "prettier-plugin-sh": "^0.18.1",
    "prettier-plugin-sql": "^0.20.0"
  }
}
